[project]
name = "sessions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymysql",
    "python-dotenv",
    "numpy",
    "matplotlib",
    "scipy",
    "powerlaw",
    "hdbscan",
    "duckdb>=1.5.5",
    "running-locally",
]

[tool.uv.sources]
running-locally = { path = "../running-locally" }

[project]
name = "running-locally"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "polars>=1.43.1",
    "pymysql>=1.2.0",
    "python-dotenv>=1.2.2",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["running_locally*"]
